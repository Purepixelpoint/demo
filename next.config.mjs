/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['buildingworld-dev.s3.ap-south-1.amazonaws.com', 'bw-production.s3.ap-south-1.amazonaws.com'],
    },
};

export default nextConfig;
